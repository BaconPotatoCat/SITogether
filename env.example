# Frontend
NODE_ENV=development

# API Configuration
NEXT_PUBLIC_FRONTEND_INTERNALURL=http://sitogether-frontend:3000
NEXT_PUBLIC_FRONTEND_EXTERNALURL=http://localhost:3000
NEXT_PUBLIC_BACKEND_INTERNALURL=http://sitogether-backend:5000

# Authentication
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# Email Configuration (Gmail only - for email verification)
# Use Gmail App Password (not your regular password)
EMAIL_USER=your-email@gmail.com
EMAIL_PASSWORD=your-app-password-here

# Database Configuration
DATABASE_URL=postgresql://sitogether_user:sitogether_password@sitogether-db:5432/sitogether_db
DB_NAME=sitogether_db
DB_USER=sitogether_user
DB_PASSWORD=sitogether_password

# Google reCAPTCHA Configuration
RECAPTCHA_SITE_KEY=your-recaptcha-site-key-here
RECAPTCHA_SECRET_KEY=your-recaptcha-secret-key-here
NEXT_PUBLIC_RECAPTCHA_SITE_KEY=your-recaptcha-site-key-here

# Encryption Configuration (for pgcrypto)
# Generate a strong key: openssl rand -base64 32
# This key is used for encrypting sensitive data at rest
ENCRYPTION_KEY=your-encryption-key-here-change-this-in-production

# This admin account will be created when seeding
# IMPORTANT: Use a real, accessible email address for 2FA authentication
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=change-this-secure-admin-password

# Logging Configuration
# Host path where logs should be stored (shared by backend and frontend).
# This path is bind-mounted into both containers at /app/logs
# Backend writes to: <LOG_DIR>/backend.log
# Frontend writes to: <LOG_DIR>/frontend.log
# Example: ./logs or /var/logs/sitogether
LOG_DIR=./logs

# Proxy Configuration
# Number of trusted proxies (e.g., 1 for single reverse proxy, 0 for no proxy)
# Default is 0. Set to 1 if behind Nginx, AWS ELB, Heroku, etc.
TRUST_PROXY=0
